enum AppRouteName {
  home('home'),
  favorite('favorite'),
  profile('profile'),
  bottomSheetDialog('bottom_sheet_dialog'),
  defaultDialog('default_dialog'),
  ;

  const AppRouteName(this.name);

  final String name;

}