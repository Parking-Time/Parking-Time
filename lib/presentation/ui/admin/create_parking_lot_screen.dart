import 'package:flutter/material.dart';

class CreateParkingLotScreen extends StatelessWidget {
  const CreateParkingLotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}
